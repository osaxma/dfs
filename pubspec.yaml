name: dfs
description: Dart Flutter Scripts (DFS) -- collection of useful scripts for Dart and Flutter projects. 
version: 0.0.3
homepage: https://github.com/osaxma
# temp until analysis_server_client null-safety is available in pub.dev
publish_to: none

environment:
  sdk: '>=2.15.0 <3.0.0'

executables:
  dfs:

dependencies:
  analyzer: ^3.0.0
  args: ^2.3.0
  cli_util: ^0.3.5
  code_builder: ^4.1.0
  dart_style: ^2.2.1
  logging: ^1.0.2
  path: ^1.8.0
  pubspec_parse: ^1.2.0
  # This is temp until this is fixed: https://github.com/dart-lang/sdk/issues/45311
  analysis_server_client: 
    git: 
      url: git://github.com/dart-lang/sdk.git
      ref: stable
      path: pkg/analysis_server_client
  git: ^2.0.0
  glob: ^2.0.2

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
